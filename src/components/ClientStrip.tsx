import type { Client } from '@/lib/cms'
export function ClientStrip({clients}:{clients:Client[]}){return <section className="pb-28"><div className="container"><div className="rule pt-6 flex justify-between gap-6"><span className="mono">Trusted by creators & brands</span><span className="text-sm text-[var(--muted)]">{clients.length ? 'Selected collaborators' : 'Add clients in Admin'}</span></div><div className="grid grid-cols-2 md:grid-cols-5 gap-x-6 gap-y-8 mt-12">{clients.slice(0,10).map(c=><div key={c.id} className="min-w-0"><p className="display font-semibold text-xl truncate">{c.name}</p>{c.metric&&<p className="mono mt-2 text-[var(--muted)]">{c.metric}</p>}</div>)}</div></div></section>}
