import Link from 'next/link'
import type { WorkItem } from '@/lib/cms'
import { WorkCard } from './WorkCard'
export function WorkGrid({work,featured=false}:{work:WorkItem[];featured?:boolean}){return <div>{featured&&<div className="flex items-end justify-between gap-6 mb-12"><div><p className="mono mb-4">Selected work</p><h2 className="display text-5xl md:text-7xl">Built for the click.</h2></div><Link href="/gallery" className="hidden sm:block rounded-full border border-black px-5 py-3 text-sm">View all work ↗</Link></div>}<div className="grid md:grid-cols-2 lg:grid-cols-3 gap-x-5 gap-y-12">{work.length?work.map(x=><WorkCard key={x.id} item={x}/>):<div className="col-span-full border border-dashed border-[var(--line)] rounded-[18px] p-12 text-center"><p className="display text-3xl">No published work yet.</p><p className="text-[var(--muted)] mt-2">Upload your first thumbnail in /admin.</p></div>}</div>{featured&&<div className="mt-10 sm:hidden"><Link href="/gallery" className="inline-block rounded-full border border-black px-5 py-3 text-sm">View all work ↗</Link></div>}</div>}
