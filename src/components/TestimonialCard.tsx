import Image from 'next/image'
import type { Testimonial } from '@/lib/cms'
export function TestimonialCard({item}:{item:Testimonial}){const avatar=item.avatar?.sizes?.thumbnail?.url||item.avatar?.url;return <article className="border border-[var(--line)] rounded-[20px] p-7 bg-white/30"><div className="flex items-center gap-3">{avatar?<Image src={avatar} alt="" width={44} height={44} className="rounded-full object-cover"/>:<div className="w-11 h-11 rounded-full bg-black/10 grid place-items-center display">{item.clientName?.[0]}</div>}<div><p className="font-semibold">{item.clientName}</p><p className="text-sm text-[var(--muted)]">{[item.clientRole,item.companyOrChannel,item.subscriberCount].filter(Boolean).join(' · ')}</p></div></div><p className="display text-2xl leading-8 mt-10">“{item.quote}”</p></article>}
